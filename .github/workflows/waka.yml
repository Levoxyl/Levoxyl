name: Waka Readme

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch: 

# FIX 1: Explicitly grant write permissions so the Action can save changes
permissions:
  contents: write

jobs:
  update-readme:
    name: Update Waka Stats
    runs-on: ubuntu-latest
    steps:
      - uses: athul/waka-readme@master
        with:
          WAKATIME_API_KEY: ${{ secrets.WAKATIME_API_KEY }}
          SHOW_TITLE: true
          SECTION_NAME: waka
          BLOCKS: "⣿"
          TIME_RANGE: all_time 
          LANG_COUNT: 10
          SHOW_TIME: true
          SHOW_TOTAL: true
        # FIX 2: Explicitly pass the GitHub token to authenticate the commit
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
